  <div class="users-list-column">
    <h2>Waiter List</h2>
    <table class="users-list">
      <thead>
        <tr>
          <th>Waiter name</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        {{#each waiters}}
          <tr>
            <td class="username">{{this.username}}</td>
            <td>
              <form action="/admin/delete-user/{{../username}}" method="post">
                <input type="hidden" name="username" value="{{this.username}}">
                <button type="submit" class="delete-button">Delete</button>
              </form>
            </td>
          </tr>
        {{/each}}
      </tbody>
    </table>

    <form action="/admin/{{username}}" method="get">
  <button type="submit">Back To admin</button>
</form>

  </div>
  <script>
  document.addEventListener('DOMContentLoaded', () => {
    const deleteButtons = document.querySelectorAll('.delete-user-btn');

    deleteButtons.forEach(button => {
      button.addEventListener('click', (e) => {
        const userId = e.target.dataset.id;
        const confirmed = window.confirm('Are you sure you want to delete this user?');

        if (confirmed) {
          setTimeout(() => {
            // Code to delete the user
            // You might need to call a function here that sends a request to the server to delete the user
            deleteUser(userId);
          }, 5000); // 5 seconds delay
        }
      });
    });
  });

  function deleteUser(userId) {
    // Code to delete the user, e.g., send a DELETE request to the server
  }
</script>
